/**
 * 判斷輸入的數字是否為正數
 * @param number - 要判斷的數字
 *
 * 注意：這個函式目前有錯誤，需要修正。正確的實現應該是判斷輸入的數字是否大於 0。
 */
/**
 * Determine whether the input number is positive
 * @param Number -Numbers to be judged
 *
 * Note: There are currently errors in this function and need to be corrected. The correct implementation should be to determine whether the input number is greater than 0.
 */
export function isPositive(number: number): boolean {
  return number > 0;
}
